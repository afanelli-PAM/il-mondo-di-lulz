<div class="live-stats-bar">
    <div class="container">
        <div class="stats-ticker">
            <span class="stat-phrase">
                ✦ Ben <strong>
                    <%= siteStats.userCount %>
                </strong> anime hanno già varcato la soglia del Mistero.
            </span>
            <span class="stat-phrase">
                ✦ <strong>
                    <%= siteStats.downloadCount %>
                </strong> cercatori di verità hanno accolto la saggezza del Libro.
            </span>
            <span class="stat-phrase">
                ✦ In questo istante, <strong>
                    <%= siteStats.visitorCount %>
                </strong> viandanti scrutano il velo delle Stelle.
            </span>
        </div>
    </div>
</div>

<style>
    .live-stats-bar {
        background: rgba(18, 18, 42, 0.85);
        border-bottom: 1px solid rgba(218, 165, 32, 0.3);
        padding: 0.5rem 0;
        font-size: 0.85rem;
        color: var(--text-muted);
        backdrop-filter: blur(8px);
        overflow: hidden;
        position: relative;
        z-index: 99;
    }

    .stats-ticker {
        display: flex;
        justify-content: space-around;
        gap: 2rem;
        white-space: nowrap;
        animation: fadeIn 1s ease-out;
    }

    .stat-phrase strong {
        color: var(--gold);
        font-weight: 700;
    }

    @media (max-width: 768px) {
        .stats-ticker {
            flex-direction: column;
            align-items: center;
            gap: 0.5rem;
            text-align: center;
        }
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(-10px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>